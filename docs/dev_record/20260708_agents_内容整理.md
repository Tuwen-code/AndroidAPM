# AGENTS 内容整理开发记录

## 1. 需求概述

整理 `AGENTS.md` 中现有 contributor guide 与强制开发文档流程，减少重复表达，使规则更清晰、可执行。

## 2. 方案设计

- 修改文件：`AGENTS.md`
- 新增文件：`docs/dev_record/20260708_agents_内容整理.md`
- 保留原有核心要求：开发前生成文档、开发后追加变更记录、中文文档、测试与提交规范。
- 将重复的编码约束合并到对应章节。

## 3. 风险与边界处理

- 不调整 Gradle 配置、源码或测试代码。
- 保留项目命令、路径和模块名，避免误导后续开发。
- 当前项目未显式配置 ktlint 等格式化工具，文档仅要求遵循现有 Kotlin/Android 风格并运行 lint。

## 4. 实现步骤

1. 创建 `docs/dev_record/` 目录。
2. 新增本开发记录。
3. 重写并整理 `AGENTS.md` 章节结构。
4. 读取文件确认中文内容正常。

## 5. 测试验证方案

- 使用 UTF-8 读取 `AGENTS.md`，确认中文显示正常。
- 检查 `git status`，确认仅新增或修改预期文档文件。

## 6. 变更记录

- 开发人：Codex
- 时间：2026-07-08
- 修改内容：创建开发记录并整理 `AGENTS.md` 内容结构。
